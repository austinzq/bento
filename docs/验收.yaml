# 交付前固定验收清单——bot 改了代码/部署或宣称完成时框架自动跑，结果贴回复末尾。
# 加一条：频道里说「验收加一条：…」；现在跑：「验收跑一遍」；看清单：「验收清单」。
检查:
  # 线上跑的就是当前代码（平台 2026-09-08 统一加）。
  # 起因：张雨纯同一件事说了 5 次，最后一句「这个你的确没有改，我重新下载了一份导出版，还是错的」——
  # bot 确实改了代码，但线上镜像停在旧版。当时全平台 0 个项目能发现这件事。
  # 用 latest 标签的项目这条会红：那不是误报，是真的没法判断线上是哪一版。
  - 名称: 线上跑的就是当前代码（镜像 tag == git HEAD）
    类型: 命令
    目录: .
    命令: python3 /home/zq/work/mm/mm-bot/tools/check_deployed_is_head.py bento
    期望: 退出码 0

  # 样式漂移（2026-09-23 张宇：「跟打地鼠一样…全文标题副标题字体颜色都不一样，反复改不对」）。
  # 实测他那份 62 页文档：1703 个文字元素、203 种样式组合，40px 标题就有 3 种几乎一样的蓝。
  # 根因是每个元素各自带样式、没有共享调色板，bot 每次只改用户指到的那一处。
  # 这条抓「肉眼分不出却值不同」的颜色簇（RGB 距离<30），刻意的蓝/橙/白对比不会被报。
  # 红了就是有漂移——报告里会写明该统一成哪个值、在哪几页。
  - 名称: 全文没有样式漂移（几乎一样却不相同的颜色）
    类型: 命令
    目录: .
    命令: python3 /home/zq/work/mm/mm-bot/tools/bento_style_lint.py workspace
    期望: 退出码 0
